//! Source-bound structure inventory cases.
//!
//! One module per claim, over the six fixtures beside them. The claims are the
//! public model and its ceilings, the closed per-language tables, the exact
//! spans and owner forests, and what a spent ceiling refuses.

mod asserts;
mod completeness;
mod fixtures;
/// What a retained Go inventory projects, and the ceiling a reader holding one
/// projects it beneath. Beside [`limits`] rather than inside it because nothing
/// here descends: the walk is already over before either claim is made.
mod go_projection;
mod limits;
mod model;
mod spans;
